---
export const prerender = true
import { getMostRecentStarYear } from '../utils/github-stars'

// Redirect /stars to /stars/[latest-year]/
return Astro.redirect(`/stars/${getMostRecentStarYear()}/`)
---